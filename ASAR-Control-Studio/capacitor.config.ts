import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asar.controlstudio',
  appName: 'ASAR Control Studio',
  webDir: 'package/mobile-app/www'
};

export default config;
